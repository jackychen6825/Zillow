import { combineReducers } from "redux";

import modal from './modal_reducer'; //can import it as anything because of export default on the associated pg

export default combineReducers({
    modal //ES6 syntax for modal: modal 
});


